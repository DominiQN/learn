package com.interviewbit.programming.string;

import sun.reflect.generics.reflectiveObjects.NotImplementedException;

public class ImplementStrStr {
    public int strStr(final String haystack, final String needle) {
        // needle 분석하기

        throw new NotImplementedException();

//        int haystackCursor = 0;
//
//        while (haystackCursor < haystack.length()) {
//
//            for (int needleCursor = 0; needleCursor < needle.length(); needleCursor++) {
//
//            }
//        }
    }
//
//    private int compare(final String haystack, final String needle, final int haystackCursor) {
//        for (int i = 0; i < needle.length(); i++) {
//            if (haystack.charAt(haystackCursor + i) != needle.charAt(i)) {
//
//            }
//        }
//    }
}
